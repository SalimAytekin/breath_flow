<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.google.mlkit.vision.demo"
    android:installLocation="auto">
    <!-- CameraX libraries require minSdkVersion 21, while this quickstart app
    supports low to 19. Needs to use overrideLibrary to make the merger tool
    ignore this conflict and import the libraries while keeping the app's lower
    minSdkVersion value. In code, will check SDK version, before calling CameraX
    APIs. -->
    <uses-sdk
        tools:overrideLibrary="
          androidx.camera.camera2, androidx.camera.core,
          androidx.camera.view, androidx.camera.lifecycle,
          com.google.mlkit.vision.segmentation.subject" />

    <uses-feature android:name="android.hardware.camera"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:icon="@drawable/logo_mlkit"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <!-- Flutter v2 embedding için gerekli -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Poz algılama modelini otomatik olarak indirmek için -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="pose"/>

        <!-- Ana aktivite olarak MainActivity'yi ayarla -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- MLKit seçim ekranı artık ana launcher değil -->
        <activity
            android:name=".EntryChoiceActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <!-- Poz algılama için gerekli aktiviteler -->
        <activity
            android:name=".java.CameraXLivePreviewActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.LivePreviewActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".preference.SettingsActivity"
            android:exported="false"/>

        <!-- YENİ: Native Exercise Coaching Activity (CameraX + Coaching Overlay) -->
        <activity
            android:name=".java.exercise_coaching.ExerciseCoachingActivity"
            android:exported="false"
            android:theme="@style/AppTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize"
            android:hardwareAccelerated="true"/>

    </application>
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

</manifest>
